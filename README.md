# Discord Cache Cleaner (MACOS)
# When clearing this it will remove existing logs that Discord is storing along with existing cache.
# dm @omaw for any problems

Run this in your Terminal

```bash
curl -s https://raw.githubusercontent.com/Eazvy/Clear-Discord-Cache-MACOS-/refs/heads/main/ClearCache.sh | bash
